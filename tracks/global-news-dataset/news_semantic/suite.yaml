schema_version: 1
id: news_semantic_v1
name: "News Semantic Search Benchmark v1"
description: "Concept-level queries that require semantic understanding beyond keyword overlap"
version: "1.0.0"

corpus:
  name: news_hunter_articles
  source: "postgres://localhost:54320/news_db (articles)"

# Query embeddings ({{embedding}} placeholder) use Qwen3-Embedding-0.6B (1024-dim),
# L2-normalised (scripts/embed_qwen3.ipynb). Document vectors live in
# article_embeddings (VECTOR(1024)); requires migrations (make migrate-up-all).
#
# Cosine is the only distance measured here. On unit vectors <->, <#> and <=>
# rank identically, since ||a-b||^2 = 2 - 2*cos(a,b), so a second operator would
# add an index and a column to the table without adding a result.
#
# Every ES block sets "size" to knn.k. Without it ES returns its default of 10
# hits regardless of k, capping AP and Bpref at 10/R.

templates:
  - id: pgvector_cosine
    query: |
      SELECT article_id AS id FROM article_embeddings
      ORDER BY embedding <=> '{{embedding}}'::vector
      LIMIT {{limit}}

queries:
  # Semantic queries test conceptual understanding: the relevant documents may
  # not share exact keywords with the query text.

  - id: sem-economic-crisis-consequences
    description: "Articles about economic downturns, recessions, job losses — broader than 'recession'"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-political-leadership-change
    description: "Leadership transitions, elections, coups — not just 'election' keyword"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-environmental-impact
    description: "Environmental consequences of human activity — beyond 'climate change'"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-tech-disruption
    description: "Technology reshaping industries — not just 'AI' or 'startup'"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-public-health-threat
    description: "Disease outbreaks, health emergencies — broader than 'pandemic'"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-military-conflict
    description: "Armed conflict, war, military operations"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-social-justice
    description: "Civil rights, protests, inequality — conceptual, not keyword-driven"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-energy-transition
    description: "Shift from fossil fuels to renewables — policy + technology angle"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-inflation-cost-of-living
    description: "Rising prices, affordability crisis, central bank response"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }

  - id: sem-diplomatic-relations
    description: "International diplomacy, treaties, sanctions"
    engines:
      pgvector-cosine:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      pgvector-exact:
        template: pgvector_cosine
        params: { embedding: "{{precomputed}}", limit: 50 }
      elasticsearch: |
        {
          "knn": {
            "field": "embedding",
            "query_vector": {{precomputed}},
            "k": 50,
            "num_candidates": 200
          },
          "size": 50
        }
