# datapipe preprocess — clean + map a raw dataset into canonical JSONL.
# All values are also overridable via flags (--input/--output/--mapping).
ENV=local

INPUT_PATH=/path/to/raw.csv
OUTPUT_PATH=dataset/canonical
MAPPING_CONFIG_PATH=configs/mappings/your_mapping.yaml
